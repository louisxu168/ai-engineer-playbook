# 第 2 章：上下文工程

[English](README.md) · **简体中文**

第 1 章告诉你「上下文是什么」。这一章问的是：**上下文该怎么管？**

因为真实 agent 一定会撞上这些问题：对话变长装不下、外部内容里藏着攻击指令、
日志里有不该发给模型的敏感信息。

---

## 本章实验

| 实验 | 主题 | 核心结论 | 状态 |
|---|---|---|---|
| [2-1 上下文压缩](2-1-compaction/README.zh-CN.md) | 装不下了怎么办：不管 / 裁剪 / 压缩 | 三条路都要付钱，你只能选在哪付；压缩质量 ≈ 压缩提示词质量 | ✅ |
| 2-2 Prompt 注入攻防 | 工具返回值里藏指令会怎样 | 📋 计划中 |
| 2-3 日志脱敏 | 哪些东西不该进上下文 | 📋 计划中 |

---

## 这一章想让你建立什么

第 1 章的三个实验都在**拆解**：把 agent 拆开，看每个零件掉了会怎样。

这一章开始**建造**：面对真实约束（窗口有限、外部内容不可信），
工程上有哪些做法，各自的代价是什么。

**2-1** 是个好起点，因为它的代价完全可量化 —— 每轮的提示词字符数直接打在屏幕上，
你能看着那根条子涨上去、又被压下来。

---

## 跑法

每个实验都是**独立文件夹**，自带全部代码：

```bash
cd 2-1-compaction
python3 agent.py            # 先看用法说明
```

不需要 API key。

---

## 对照原书

本章对应《深入理解 AI Agent》第 2 章（共 9 个配套实验）。
**代码是独立重写的**，选题也做了取舍：

| 本仓库 | 原书对应 | 说明 |
|---|---|---|
| 2-1 上下文压缩 | context-compression | 换了任务和实现；加了可量化的字符数可视化 |
| 2-2 Prompt 注入（计划中） | prompt-injection | |
| 2-3 日志脱敏（计划中） | log-sanitization | |

**原书里这几个我暂时不做**：`attention_visualization`、`kv-cache`、
`local_llm_serving` —— 它们需要下载本地模型权重、跑 GPU 推理，
和本仓库「零 API key、clone 下来就能跑」的前提直接冲突。想学这几个建议直接读原书。

← [返回总目录](../../README.zh-CN.md)
